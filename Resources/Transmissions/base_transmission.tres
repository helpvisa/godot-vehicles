[gd_resource type="Resource" load_steps=2 format=3 uid="uid://bd2ebcnarl2c"]

[sub_resource type="GDScript" id="GDScript_0kn65"]
script/source = "extends Resource

# public / settable
@export var gears: Array[float]
@export var finalDrive: float
@export var transmissionLoss: float

# local
var currentGear: int # 0 is reverse

func _init(p_finalDrive = 3.42, p_transmissionLoss = 0.7, p_currentGear = 1):
	finalDrive = p_finalDrive
	transmissionLoss = p_transmissionLoss
	currentGear = p_currentGear
"

[resource]
script = SubResource("GDScript_0kn65")
gears = Array[float]([-6.0, 5.8])
finalDrive = 3.42
transmissionLoss = 0.7
